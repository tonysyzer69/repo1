---
name: notify_user
pack: chatops
runner_type: "local-shell-script"
description: notify user when slack run
enabled: true
entry_point: notify_user.sh
parameters:
  user:
    type: string
    description: user
    required: true
  execution_id:
    type: "string"
    description: "Id of execution to format"
    required: true
